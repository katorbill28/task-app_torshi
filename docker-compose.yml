services:
  server:
    build:
      context: ./server
    ports:
      - "4000:4000"

  client:
    build:
      context: ./client
    environment:
      - VITE_API_URL=http://localhost:4000
    ports:
      - "5173:5173"
    depends_on:
      - server
